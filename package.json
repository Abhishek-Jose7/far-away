{
  "name": "transitiq-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:worker": "pnpm --filter worker dev",
    "dev:web": "pnpm --filter web dev",
    "db:migrate:local": "pnpm --filter worker db:migrate:local",
    "db:seed:local": "pnpm --filter worker db:seed:local"
  },
  "devDependencies": {
    "turbo": "^1.12.4",
    "typescript": "^5.3.3"
  }
}
